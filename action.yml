name: 'aws-ssm-parameters'
description: 'load ssm parameters from aws ssm'
inputs:
  paths:
    required: true
  recursive:
    default: true
  with-decryption:
    default: true
  region:
    default: 'us-east-1'
runs:
  using: 'node12'
  main: 'dist/index.js'